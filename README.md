# Filter dumps from https://spansh.co.uk/dumps

## Use

```console
$ ./spansh
Description:
  Filter JSON streamed from spansh galaxy files.

Arguments:
  'GALAXY PATH' is the path to the GALAXY file.

Usage:
  ./spansh

Examples:
  ./spansh galaxy.json.gz
```
